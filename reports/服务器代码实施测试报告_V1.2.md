# 庆喜儿服务器代码实施测试报告 V1.2

**报告日期：** 2026-07-17  
**代码分支：** `agent/qingxier-v1-1-solution-tests`  
**实现版本：** `qingxier-speech-server 1.2.0`  
**最终复验提交：** `babd1148b38e4b1474d9bfc0af7eb3178585ea3b`  
**代码测试结论：** **PASS**  
**生产验收结论：** **可进入集成测试；真实语音、数据库、MQTT、设备和长稳验收尚未完成**

## 1. 实施范围

本轮将 V1.1 方案中与新需求直接相关的服务器能力实现为可运行 FastAPI 服务：

1. 自动方言能力查询、设备语音配置、配置版本乐观锁及 MQTT 配置同步事件；
2. “阿西”唤醒后的语音会话、WakeNet9 模型版本观测和旧固件标识；
3. 普通话、闽南语、吴语、粤语、四川话、陕西话、河南话、上海话的置信度路由、会话滞回与低置信度兜底；
4. CosyVoice 克隆音色主路由、泛吴语映射上海话的显式降级、Qwen 系统音色兜底；
5. Fun-ASR-Realtime SDK 适配、可解释文本方言分类兜底及 WebSocket 语音协议；
6. 生产 HS256 JWT、用户/设备/管理员权限隔离和管理员 MFA 门槛；
7. 唤醒词模型包的字段、SHA-256、版本、回滚、幂等和审计；
8. MongoDB/内存仓储、MQTT/内存事件、OpenAI 兼容 LLM、Docker、Compose、健康检查和 CI；
9. REST 与 WebSocket 共用的安全保护层：克隆音色绑定、TTS 模型白名单、就绪降级、幂等键边界及统一 404/405。

微信支付、商城、订单及运营后台页面等既有业务域不在本轮新增需求代码范围内。

## 2. 测试环境与命令

| 项目 | 环境 |
|---|---|
| CI 系统 | Linux 6.17.0 Azure x86_64 |
| CI Python | 3.12.13 |
| 框架 | FastAPI、Uvicorn、Pydantic v2 |
| 测试 | pytest、pytest-cov、FastAPI TestClient |
| 外部服务替身 | HTTP MockTransport、伪 DashScope SDK 回调、确定性 TTS/LLM Provider |
| 真实进程冒烟 | Uvicorn `127.0.0.1:8765` |

```bash
cd server
pytest -q \
  --junitxml=../reports/server-junit.xml \
  --cov=qingxier_server \
  --cov-report=term-missing \
  --cov-report=xml:../reports/server-coverage.xml \
  --cov-fail-under=80
```

## 3. 最终结果总览

| 指标 | 结果 |
|---|---:|
| 服务器自动化用例 | 75 |
| PASS | 75 |
| FAIL / ERROR / SKIP | 0 / 0 / 0 |
| 行覆盖率 | 83.52% |
| CI 覆盖率门槛 | 80% |
| Uvicorn 进程级冒烟 | 3/3 PASS |
| 原 V1.1 契约与小程序状态流回归 | 56/56 PASS |
| P0/P1 未解决代码缺陷 | 0 |

最终 GitHub Actions 运行：

- `Server implementation tests`：SUCCESS；
- `Contract tests`：SUCCESS；
- 服务器测试 Artifact：`qingxier-server-test-evidence`；
- JUnit：75 项、失败 0、错误 0、跳过 0；
- 覆盖率：958/1147 行，83.52%。

## 4. 分类别功能与接口测试

| 分类 | 用例数 | 主要验证内容 | 结果 |
|---|---:|---|---|
| 基础服务与统一响应 | 5 | 健康、就绪、Trace ID、404 跟踪 | PASS |
| 鉴权与权限隔离 | 4 | 用户/设备/管理员 Token、MFA | PASS |
| 能力与设备语音配置 | 8 | 方言能力、配置查询修改、MQTT 事件、乐观锁 | PASS |
| 语音会话与方言路由 | 10 | “阿西”、旧固件、16k PCM、高/中/低置信度策略 | PASS |
| TTS 主备路由 | 8 | CosyVoice 克隆音色、方言指令、吴语降级、Qwen 兜底 | PASS |
| 唤醒词模型包管理 | 7 | WakeNet9、SHA-256、回滚、重复版本、幂等、审计 | PASS |
| 文字对话兼容 | 2 | LLM 调用、克隆音色回复、鉴权 | PASS |
| 生产 JWT、启动配置与仓储 | 11 | HS256、过期 Token、依赖配置、版本冲突 | PASS |
| WebSocket、ASR 与方言分类 | 6 | start/audio/ASR/TTS/ping/end、断连与协议错误 | PASS |
| 第三方服务适配器 | 7 | DashScope CosyVoice/Qwen 请求、Fun-ASR 回调、LLM 请求 | PASS |
| 安全与错误边界加固 | 7 | 404/405、存储就绪异常、音色绑定、模型白名单、幂等键 | PASS |
| **合计** | **75** |  | **PASS** |

## 5. 覆盖率

| 模块 | 行覆盖率 |
|---|---:|
| `api.py` | 100.00% |
| `asr.py` | 95.24% |
| `config.py` | 94.38% |
| `domain.py` | 98.82% |
| `errors.py` | 91.30% |
| `hardened_service.py` | 100.00% |
| `integrations.py` | 76.42% |
| `repository.py` | 62.44% |
| `security.py` | 83.50% |
| `services.py` | 87.50% |
| `websocket_api.py` | 76.32% |
| **整体** | **83.52%** |

未覆盖代码主要是需要真实网络依赖的 MongoDB、MQTT、供应商异常分支和直接启动入口。CI 每次重新生成 `server-coverage.xml` 并作为 `qingxier-server-test-evidence` Artifact 上传。

## 6. 进程级冒烟测试

独立启动 Uvicorn 后执行：

| 检查 | 结果 |
|---|---|
| `GET /health` 返回 HTTP 200 / `status=ok` | PASS |
| `GET /api/v1/speech/capabilities` 用户鉴权及能力返回 | PASS |
| `POST /api/v1/speech/sessions` 设备鉴权并建立“阿西”会话 | PASS |

GitHub Actions 中 `Process-level HTTP smoke test` 步骤结论为 SUCCESS。

## 7. 测试发现与修复记录

| 编号 | 发现 | 修复 | 复测 |
|---|---|---|---|
| FIX-01 | Pydantic 错误可能返回框架默认结构 | 统一为 `400001 INVALID_ARGUMENT` | PASS |
| FIX-02 | 初始覆盖率未达到交付门槛 | 增加生产配置、适配器和异常测试，门槛提升到 80% | 83.52% PASS |
| FIX-03 | WebSocket 主动断连后可能继续 `receive` | 显式处理 disconnect 并清理 ASR 会话 | PASS |
| FIX-04 | WebSocket 非法 TTS/方言参数可能抛未处理 ValidationError | 转换为可恢复协议错误帧 | PASS |
| FIX-05 | Fun-ASR 回调错误原因可能丢失 | ASR 事件增加 error 并映射为 `503101` | PASS |
| FIX-06 | WebSocket 内同步 ASR/TTS/仓储调用可能阻塞事件循环 | 使用 `asyncio.to_thread` 隔离 | PASS |
| FIX-07 | JWT `exp` 非数值可能导致 500 | 显式校验并返回 401 | PASS |
| FIX-08 | 框架级 404/405 未使用项目标准错误体 | 增加 Starlette HTTP 异常统一映射 | PASS |
| FIX-09 | 请求可覆盖设备绑定的克隆音色或指定未批准模型 | REST/WSS 共用安全服务层，校验音色绑定与模型白名单 | PASS |
| FIX-10 | 过长或空白幂等键可能污染幂等存储 | 标准化并限制为 1–128 字符 | PASS |
| FIX-11 | 仓储 `ping` 抛错时 `/ready` 可能返回通用 500 | 捕获适配器异常并稳定返回 `503000 NOT_READY` | PASS |

## 8. 关键规则验证结论

- 普通用户不能关闭自动方言识别；
- 高置信度锁定候选方言，中置信度沿用会话方言，低置信度使用配置兜底；
- Qwen3-TTS 系统音色模型不能与克隆 `voiceId` 组合；
- 客户端显式提交的克隆 `voiceId` 必须与当前设备绑定值一致，REST 与 WebSocket 口径一致；
- 客户端只能使用服务端批准的 CosyVoice 主模型或 Qwen 系统音色降级模型；
- CosyVoice 克隆音色请求携带方言 instruction；泛吴语映射上海话时返回明确降级原因；
- 克隆合成失败时按配置降级到系统音色，主备均失败返回 `503102`；
- 用户只可访问已绑定设备，设备只可访问自身会话，管理员写操作需 MFA；
- 语音配置通过版本字段防并发覆盖；
- 唤醒词包必须为“阿西”/WakeNet9，并校验 SHA-256、版本、回滚和幂等行为；
- 未知资源、错误方法、参数错误与业务错误均返回统一错误体及 Trace ID；
- WebSocket 支持完整会话、协议错误和断连清理。

## 9. 尚需真实环境执行

以下项目没有用 Mock 结果冒充通过：

1. 正式 DashScope API Key、克隆 voiceId 和七类方言语料下的 ASR 准确率、方言判定、TTS 可懂度、自然度及音色相似度；
2. MongoDB 副本集、索引、备份恢复、故障切换和真实并发；
3. EMQX MQTTS、ACL、QoS 1、离线补偿和重投；
4. 公网 WSS、证书、反向代理、连接数、限流、背压和容量；
5. 生产 JWT 签发、可信 MFA 网关、密钥轮换和渗透测试；
6. 压力、故障注入和 72 小时稳定性；
7. ESP32-S3 真机、WakeNet9 “阿西”唤醒率/误唤醒率及 OTA 回滚。

## 10. 结论与证据

服务器代码最终自动化结论为：**75/75 PASS，覆盖率 83.52%，Uvicorn 冒烟 3/3 PASS；原 V1.1 契约回归 56/56 PASS。** 可以合入集成测试分支并部署到具备正式测试凭据的环境，但不能据此宣称生产语音质量、设备或 72 小时长稳验收已经通过。

仓库证据：

- `server/qingxier_server/`
- `server/tests/`
- `reports/服务器代码实施测试报告_V1.2.md`
- `reports/server-test-summary.json`
- `.github/workflows/server-tests.yml`
- GitHub Actions Artifact：`qingxier-server-test-evidence`
