import{randomUUID}from'node:crypto';import type{Entity}from'@/domain/types';export const now=()=>new Date().toISOString();export const id=(p:string)=>`${p}_${randomUUID().replaceAll('-','').slice(0,16)}`;export const entity=<T extends Record<string,any>>(p:string,v:T):T&Entity=>{const t=now();return{id:id(p),createdAt:t,updatedAt:t,...v}as T&Entity};export const sortDesc=<T extends{createdAt:string}>(xs:T[])=>[...xs].sort((a,b)=>b.createdAt.localeCompare(a.createdAt));
