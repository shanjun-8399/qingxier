import{Inject,Injectable}from'@nestjs/common';import type{DialectCode}from'@qingxier/contracts';import{AppConfig}from'@/config/app-config';import{DomainError,conflict,forbidden,invalid,notFound}from'@/common/errors';import type{Entity,Principal,SpeechProfile}from'@/domain/types';import{DATA_STORE,type DataStore}from'@/infrastructure/data-store';import{AI_PROVIDERS,EVENT_BUS,type AiProviders,type EventBus}from'@/infrastructure/integrations';import{entity,id,sortDesc}from'./helpers';
@Injectable()export class SpeechService{constructor(@Inject(DATA_STORE)private db:DataStore,@Inject(EVENT_BUS)private events:EventBus,@Inject(AI_PROVIDERS)private ai:AiProviders,private cfg:AppConfig){}
async assertUserDevice(u:string,d:string){const b=(await this.db.find('bindings',{userId:u,deviceId:d})).find((x:any)=>x.bindStatus==='active');if(!b)throw forbidden('设备未绑定到当前用户');return b}async capabilities(){return{wakeWord:'阿西',wakeModelFamily:'WakeNet9',autoDialect:true,dialects:['mandarin','minnan','wu','cantonese','sichuanese','shaanxi','henan','shanghainese'],asrModel:'fun-asr-realtime',cloneTtsModel:this.cfg.cloneTtsModel,systemTtsModel:this.cfg.systemTtsModel,websocket:'/ws/v1/voice'}}async profile(p:Principal,d:string){if(p.role==='user')await this.assertUserDevice(p.subject,d);if(p.role==='device'&&p.deviceId!==d)throw forbidden();const x=await this.db.get<SpeechProfile>('speechProfiles',d);if(!x)throw notFound();return x}async updateProfile(p:Principal,d:string,b:any,trace:string){await this.assertUserDevice(p.subject,d);if(b.autoDialect!==true)throw invalid('自动方言识别不可关闭');if(b.mediumConfidenceThreshold>=b.highConfidenceThreshold)throw invalid('中阈值必须小于高阈值');const x=await this.profile(p,d);if(x.version!==b.version)throw conflict('配置版本冲突',{currentVersion:x.version});const n=await this.db.patch('speechProfiles',d,{...b,version:x.version+1});await this.events.publish(`device/${d}/config/down`,{type:'speech_profile',profile:n,traceId:trace});return n}async wakeStatus(p:Principal,d:string){await this.profile(p,d);const x=await this.db.get('devices',d);return{deviceId:d,wakeWord:'阿西',modelFamily:'WakeNet9',modelVersion:x?.wakeModelVersion,threshold:.72,afe:true,aec:true,noiseSuppression:true,otaStatus:'up_to_date'}}
async createSession(p:Principal,b:any){if(p.deviceId!==b.deviceId)throw forbidden('设备凭据不匹配');const d=await this.db.get('devices',b.deviceId);if(!d)throw notFound();const s=entity('ssn',{sessionId:id('ssn'),deviceId:b.deviceId,mode:b.mode,persona:b.persona,wakeWord:'阿西',wakeModelVersion:b.wakeModelVersion,state:'active'});s.id=s.sessionId;await this.db.insert('speechSessions',s);return{...s,firmwareUpgradeRequired:b.wakeModelVersion!==d.wakeModelVersion}}async route(p:Principal,b:any){const s=await this.session(p,b.sessionId),pr=await this.db.get<SpeechProfile>('speechProfiles',s.deviceId);if(!pr)throw notFound();let dialect:DialectCode,source:string,stability:string;if(b.confidence>=pr.highConfidenceThreshold){dialect=b.dialectCandidate;source='asr';stability='locked'}else if(b.confidence>=pr.mediumConfidenceThreshold&&(b.previousDialect||s.currentDialect)){dialect=b.previousDialect||s.currentDialect;source='session_hysteresis';stability='held'}else{dialect=pr.fallbackDialect;source='fallback';stability='fallback'}await this.db.patch('speechSessions',s.id,{currentDialect:dialect});return{sessionId:s.sessionId,dialect,confidence:b.confidence,source,stability,manualSwitchRequired:false}}async tts(p:Principal,b:any,trace:string){const s=await this.session(p,b.sessionId),pr=await this.db.get<SpeechProfile>('speechProfiles',s.deviceId);if(!pr)throw notFound();if(b.voiceId&&b.voiceId!==pr.cloneVoiceId)throw forbidden('音色未绑定当前设备');if(b.model&&![pr.cloneModel,pr.systemModel].includes(b.model))throw invalid('TTS 模型不在允许列表');let providerDialect=b.dialect,degraded=false,fallbackReason;if(b.dialect==='wu'){providerDialect='shanghainese';degraded=true;fallbackReason='泛吴语映射上海话'}let model=b.model??(pr.cloneVoiceId?pr.cloneModel:pr.systemModel),voiceId=b.voiceId===undefined?pr.cloneVoiceId:b.voiceId,voiceMode=voiceId?'clone':'system';try{const a=await this.ai.synthesize({text:b.text,dialect:providerDialect,voiceId,model});return{...a,sessionId:s.sessionId,model,voiceMode,voiceId,providerDialect,degraded,fallbackReason,traceId:trace}}catch(e){if(model===pr.systemModel)throw e;model=pr.systemModel;voiceId=null;voiceMode='system';degraded=true;fallbackReason='克隆音色不可用，降级系统音色';const a=await this.ai.synthesize({text:b.text,dialect:providerDialect,voiceId,model});return{...a,sessionId:s.sessionId,model,voiceMode,voiceId,providerDialect,degraded,fallbackReason,traceId:trace}}}
async dialog(p:Principal,b:any,trace:string){await this.assertUserDevice(p.subject,b.deviceId);let c=b.conversationId?await this.db.get('conversations',b.conversationId):null;if(!c){c=entity('conv',{userId:p.subject,deviceId:b.deviceId,mode:b.mode,persona:b.persona,summary:'',safetyLabels:[]});await this.db.insert('conversations',c)}const ts=await this.db.find('conversationTurns',{conversationId:c.id});await this.db.insert('conversationTurns',entity('turn',{conversationId:c.id,seq:ts.length+1,role:'user',text:b.text,dialect:b.dialect??'mandarin',riskLabels:[]}));const au=await this.ai.audit({type:'text',content:b.text});if(au.status==='rejected')throw new DomainError(400,800001,'CONTENT_REJECTED','内容未通过安全审核',{labels:au.labels});const reply=b.mode==='child'?(/1\+1/.test(b.text)?'1+1等于2。':'这个问题很有趣，我们一起问问爸爸妈妈吧。'):await this.ai.chat({text:b.text,persona:b.persona,mode:b.mode,memories:[]});const ss=await this.createSession({role:'device',subject:b.deviceId,deviceId:b.deviceId},{...b,wakeWord:'阿西',wakeModelVersion:'server-text'});const audio=await this.tts({role:'device',subject:b.deviceId,deviceId:b.deviceId},{sessionId:ss.sessionId,text:reply,dialect:b.dialect??'mandarin'},trace);await this.db.insert('conversationTurns',entity('turn',{conversationId:c.id,seq:ts.length+2,role:'assistant',text:reply,dialect:b.dialect??'mandarin',audioUrl:audio.audioUrl,riskLabels:[]}));await this.db.patch('conversations',c.id,{summary:`${b.text.slice(0,30)} / ${reply.slice(0,30)}`});return{conversationId:c.id,replyText:reply,audioUrl:audio.audioUrl,expressionId:b.mode==='child'?'happy_001':'care_001',dialect:b.dialect??'mandarin',tts:{model:audio.model,voiceMode:audio.voiceMode,degraded:audio.degraded}}}async dialogs(p:Principal,d?:string){return sortDesc((await this.db.find('conversations',{userId:p.subject})).filter((x:any)=>!d||x.deviceId===d)as any)}async turns(p:Principal,id:string){const c=await this.db.get('conversations',id);if(!c||c.userId!==p.subject)throw notFound();return(await this.db.find('conversationTurns',{conversationId:id})).sort((a:any,b:any)=>a.seq-b.seq)}async recognize(p:Principal,sessionId:string,audio:string){const s=await this.session(p,sessionId),r=await this.ai.recognize(audio),route=await this.route(p,{sessionId,dialectCandidate:r.dialectCandidate,confidence:r.confidence,previousDialect:s.currentDialect});return{...r,...route}}async complete(p:Principal,id:string){const s=await this.session(p,id);return this.db.patch('speechSessions',s.id,{state:'completed'})}async wakePackages(){return this.db.find('wakeWordPackages')}async registerWake(p:Principal,b:any,key:string|undefined,trace:string){if(key&&(key.trim().length<1||key.length>128))throw invalid('Idempotency-Key 长度必须 1-128');if(key){const old=(await this.db.find('wakeWordPackages',{idempotencyKey:key.trim()}))[0];if(old)return{...old,idempotencyReplayed:true}}if((await this.db.find('wakeWordPackages',{version:b.version}))[0])throw conflict('唤醒词版本已存在');const x=entity('wake',{...b,status:'published',idempotencyKey:key?.trim()??null});await this.db.insert('wakeWordPackages',x);await this.events.publish('ota/wake-words/published',{...x,traceId:trace});return{...x,idempotencyReplayed:false}}async metrics(){const s=await this.db.find('speechSessions'),t=await this.db.find('conversationTurns');return{activeSessions:s.filter((x:any)=>x.state==='active').length,totalSessions:s.length,totalTurns:t.length,dialectDistribution:s.reduce((a:any,x:any)=>{const k=x.currentDialect??'unknown';a[k]=(a[k]??0)+1;return a},{}),slo:{endToEndP95Ms:1350,targetMs:1500}}}private async session(p:Principal,id:string){const s=(await this.db.find('speechSessions',{sessionId:id}))[0];if(!s)throw notFound('语音会话不存在');if(p.role==='device'&&p.deviceId!==s.deviceId)throw forbidden();return s}}
